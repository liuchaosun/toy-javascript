function _Notification(title, options) {
  let {
    dir, // 显示通知的方向
    lang, // 通知的语言
    badge,
    body, // 显示主体新闻
    tag,
    icon, // 显示图标
    image, // 显示图片
    data,
    vibrate,
    renotify, // 新通知覆盖老通知时是否通知
    requireInteraction, // 是否
    silent, // 是否静音
  } = options;
}
